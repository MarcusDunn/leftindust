schema: http://192.168.0.11:8080/graphql

generates:
  ./src/api/server/schemas/leftindust.schema.ts:
    hooks:
      afterOneFileWrite:
        - eslint --fix
    plugins:
      - typescript
      - typescript-operations
    config:
      maybeValue: T | undefined  

  ./src/api/server/schemas/leftindust.schema.graphql:
    plugins:
      - schema-ast
