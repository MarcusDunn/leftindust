#import "./namesFragment.graphql"

fragment users on User {
    __typename
    uid
    isRegistered
    group {
        name
    }
    firebaseUserInfo {
        email
    }
    names {
        ...names
    }
}
