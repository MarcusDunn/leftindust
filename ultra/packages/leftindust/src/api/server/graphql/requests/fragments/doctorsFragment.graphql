#import "./emailFragment.graphql"

fragment doctors on Doctor {
    __typename
    # Make this into a fragment once server-side is updated
    firstName
    middleName
    lastName
    title
    did {
        id
    }
    emails {
        ...email
    }
}
