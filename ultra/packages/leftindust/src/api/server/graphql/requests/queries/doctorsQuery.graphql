#import "../fragments/doctorsFragment.graphql"

query doctorsQuery(
    $range: RangeInput,
    $dids: [DoctorIdInput!],
    $example: DoctorExampleInput,
    $pid: PatientIdInput,
) {
    doctors(range: $range, dids: $dids, example: $example, pid: $pid) {
        ...doctors
    }
}
